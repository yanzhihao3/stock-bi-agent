# ============================================================
# Docker 构建上下文排除清单
# docker build 时，下面这些东西不会被送进镜像。构建上下文 = 项目根目录。
#
# 最重要的一条：.env 和数据库文件绝不能进镜像。
# 没有这个文件的话，Dockerfile 里的 COPY . . 会把你本地的真实 API Key
# 和两个 SQLite 数据库原样打进镜像层，镜像一旦推送到任何地方就等于泄密。
# ============================================================

# ---- 密钥与配置：绝不进镜像，只保留模板 ----
.env
.env.*
!.env.example

# ---- 本地数据：用户表、对话记录、备份，运行时全部走卷挂载 ----
assert/
backups/
*.db
*.sqlite
*.db-journal

# ---- 版本控制与 CI 配置 ----
.git
.gitignore
.github/

# ---- Python 缓存与构建产物 ----
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---- 虚拟环境（镜像里会重新装依赖，不需要本地的） ----
.venv/
venv/
env/

# ---- IDE 与系统文件 ----
.idea/
.vscode/
.DS_Store
Thumbs.db

# ---- 个人文档与日志 ----
docs/
*.log
